// ============================================================
// การตั้งค่าแดชบอร์ดวาระคงเหลือ
// ------------------------------------------------------------
// แก้ไขค่าที่นี่เพื่อเปลี่ยนพฤติกรรมเริ่มต้น
// (ผู้ใช้ยังสามารถปรับ "เกณฑ์ใกล้หมดวาระ" และ "วันที่อ้างอิง"
//  ได้จากหน้าเว็บผ่านปุ่ม "ตั้งค่า" — ค่านั้นจะถูกบันทึกใน
//  localStorage และมีผลเหนือค่าที่กำหนดที่นี่)
// ============================================================

export const CONFIG = {
  // โซนเวลาที่ใช้หาวันนี้และแสดงนาฬิกา
  timezone: 'Asia/Bangkok',

  // วันที่อ้างอิงของข้อมูล — วันที่ที่ค่า "วาระคงเหลือ" ในไฟล์ CSV ถูกวัด
  //
  // ชุดข้อมูลระบุวาระคงเหลือเป็น "ระยะเวลาที่เหลือ" เช่น "5 ปี 1 เดือน"
  // ไม่ใช่วันที่สิ้นสุด ดังนั้น:
  //     วันที่สิ้นสุดวาระ = วันที่อ้างอิง + ระยะเวลาวาระคงเหลือ
  //
  // ค่าเริ่มต้นใช้วันที่แก้ไขไฟล์ต้นฉบับ (13 ส.ค. 2569)
  // หากทราบวันที่เก็บข้อมูลจริง ให้แก้ไขเป็นค่านั้น เช่น '2026-07-13'
  // (รองรับทั้ง ISO "2026-07-13", ไทย "13/7/2569", และ "13 ก.ค. 2569")
  dataReferenceDate: '2026-08-13',

  // เกณฑ์ "ใกล้หมดวาระ" (วัน) — จำนวนวันคงเหลือ <= ค่านี้
  expiringSoonThresholdDays: 30,

  // ตำแหน่งไฟล์ข้อมูล CSV (เข้ารหัส UTF-8)
  csvUrl: '/api/source/vara-csv',

  // ป้ายชื่อและสีของแต่ละสถานะ
  status: {
    active: { label: 'ดำรงวาระ', short: 'Active', color: '#16a34a' },
    expiring: { label: 'ใกล้หมดวาระ', short: 'Expiring Soon', color: '#d97706' },
    expired: { label: 'หมดวาระแล้ว', short: 'Expired', color: '#dc2626' },
    invalid: { label: 'ข้อมูลไม่สมบูรณ์', short: 'Invalid', color: '#64748b' },
  },
};
